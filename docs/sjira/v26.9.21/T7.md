---
id: T7
dcterms:title: "Verification: cache normalization + rpath"
dcterms:created: "2026-09-21"
dcterms:isPartOf: "v26.9.21"
rdf:type: oslc_cm:ChangeRequest, earl:TestRequirement
aps:standing: https://w3id.org/chatman/aps#PARTIAL_ALIVE
milestone: frozen-duckdb v1.5.5 (DuckDB 1.5.5 support, closes seanchatmangpt/frozen-duckdb#1)
worktree: /Users/sac/frozen-duckdb-wt/verify
branch: feat/155-verify
---

# T7 — Verification: cache normalization + rpath

## Scope (files you may change)

```
NO repo edits; owns mutation of ~/.frozen-duckdb/cache/v1.5.5-arm64 (announce in History before mutating)
```

Anything outside scope: do not edit — record BLOCKED with the reason instead.

## Work order


Context: prove the builder's normalization and rpath propagation by execution. (1) Delete ~/.frozen-duckdb/cache/v1.5.5-arm64/duckdb/ and libduckdb.dylib → cargo build -p frozen-duckdb → assert both restored (vendored headers + symlink) and build exit 0. (2) otool -l target/debug/frozen-duckdb-cli | grep -A2 LC_RPATH shows the cache dir. (3) env -i HOME=/Users/sac PATH=/usr/bin:/bin <abs> frozen-duckdb-cli --help → exit 0 (clean-env dyld proof). (4) python3 ctypes duckdb_library_version() == v1.5.5 post-mutation. (5) cd test-dependency && cargo build → dual-engine attempt, record outcome (UNKNOWN acceptable, preserve output).
DoD: [ ] all five probes executed with command+exit recorded in History  [ ] standing declared

## Contract (all tickets)

- Work ONLY in `/Users/sac/frozen-duckdb-wt/verify` on `feat/155-verify`. NEVER touch the main checkout /Users/sac/frozen-duckdb or sibling worktrees. Never push, never touch remotes.
- Atomic commits, conventional messages (`test:`, `fix:`, `docs:`, `ci:`, `build:`), on your branch only.
- 流: read this ticket → 階 (reuse what exists) → edit → verify (run the DoD gates) → 証 (append History) → stop.
- Standing vocabulary: ALIVE | BLOCKED | BUILD_BROKEN | PARTIAL_ALIVE | REFUSED_* | UNKNOWN | UNSUPPORTED. Preserve every command + exit code. inspection ≠ execution: a gate you did not run is not green.
- Shared resources: ~/.frozen-duckdb/cache (read-mostly; T7 owns its v1.5.5-arm64 mutation), crates.io registry, network. Coordinate through the ticket, never by touching sibling trees.
- Update the History table on every transition, then STOP — the coordinator merges.

## History

| ts | standing | branch+SHA | gates+exits | remaining |
|----|----------|------------|-------------|-----------|
| 2026-09-21 | OPEN | feat/155-verify@c8e5788 | not started | all of DoD |
| 2026-09-21 14:03 | OPEN | feat/155-verify@f048742 | ANNOUNCE sanctioned mutation (ticket scope): rm -rf ~/.frozen-duckdb/cache/v1.5.5-arm64/duckdb/ AND rm ~/.frozen-duckdb/cache/v1.5.5-arm64/libduckdb.dylib — libduckdb_arm64.dylib KEPT — then probe 1 restoration via cargo build -p frozen-duckdb; T7 owns this mutation | probes 1-5 + receipt |
| 2026-09-21 14:06 | ALIVE (P1-P4) | feat/155-verify@f048742 | P1 exit 0 `cargo build -p frozen-duckdb` (21:03:56Z-21:05:25Z) after deletion confirmed absent 14:03:40 → duckdb/duckdb.h RESTORED (vendored copy from crates/frozen-duckdb-builder/vendored-headers/duckdb.h; ensure_headers) AND libduckdb.dylib RESTORED as symlink → libduckdb_arm64.dylib (ensure_link_name); dir+symlink created 14:05:13 inside MY build window = THIS build restored them (frozen-duckdb-sys recompiled, log: "Using prebuilt DuckDB binary .../libduckdb_arm64.dylib"). P2 exit 0 `otool -l target/debug/frozen-duckdb-cli \| grep -A2 LC_RPATH` → path /Users/sac/.frozen-duckdb/cache/v1.5.5-arm64. P3 exit 0 `env -i HOME=$HOME PATH=/usr/bin:/bin .../target/debug/frozen-duckdb-cli --help` (dyld @rpath resolution with no env help). P4 exit 0 python3 ctypes on libduckdb_arm64.dylib → duckdb_library_version() == 'v1.5.5' MATCH (post-mutation) | P5 + receipt |
| 2026-09-21 14:08 | PARTIAL_ALIVE | feat/155-verify@f048742 | P5 exit 101 `cd test-dependency && cargo build` (21:07:20Z) = BUILD_BROKEN: "error: current package believes it's in a workspace when it's not: current: /Users/sac/frozen-duckdb-wt/verify/test-dependency/Cargo.toml, workspace: /Users/sac/frozen-duckdb-wt/verify/Cargo.toml" (cargo hint: add to workspace.members, or workspace.exclude, or empty [workspace] in package manifest). NOT FIXED — out of T7 scope per ticket; error preserved here. RECEIPT: no repo edits, no commits (build artifacts + Cargo.lock in worktree only), no push, no sibling-tree touches; cache mutation executed exactly as announced, both targets restored by observed build. Standing: PARTIAL_ALIVE — P1-P4 ALIVE (normalization + rpath + clean-env dyld + version, all observed this session), P5 BUILD_BROKEN (dual-engine consumption blocked on workspace membership; UNKNOWN whether link-phase would pass once unblocked) | none — all five probes executed with exits recorded; coordinator merges |
