---
id: C4
dcterms:title: "dry-run-publish-pack composition"
dcterms:created: "2026-09-21"
dcterms:isPartOf: "v26.9.21"
rdf:type: oslc_cm:ChangeRequest, earl:TestRequirement
aps:standing: https://w3id.org/chatman/aps#ALIVE
milestone: frozen-duckdb v1.5.5 wave 4 (marketplace capability composition)
worktree: /Users/sac/frozen-duckdb-wt/c4
branch: feat/155-c4
---

# C4 — dry-run-publish-pack composition

## 階 law (search ladder, before any write)

REUSE the pack as-is → COMPOSE packs → EXTEND a pack family → INVENT only as an
admitted pack. Record every failed edge in your ticket History: failed(edge) ≠
failed(G); silent pruning forbidden.

## Scope

```
ggen.toml; schema/; docs/sjira/v26.9.21/PUBLISH_RUNBOOK.md; gates/
```

Anything outside scope: do not edit — ledger it instead.

## Work order


Pack: packs/dry-run-publish-pack — the release DoD as a 6-phase PDDL8 domain (scope, generate, verify, manufacture, cleanroom, receipt) with a DRY-RUN-OVERCLAIM FENCE (no atom named published/crates-io-uploaded; terminal goal is dry-run-verified). Compose it onto this repo: align PUBLISH_RUNBOOK.md phases to the pack's 6 gates, render its fixtures/shapes as repo-side evidence templates, keep the fence law visible. This makes the operator's publish dry-run a repeatable modeled gate, not a one-off.
DoD: [ ] pack declared  [ ] 6-phase alignment rendered/reconciled with the runbook  [ ] fence law present in the rendered artifact  [ ] sync exit 0  [ ] committed

## Contract (all tickets)

- ggen consumer bootstrap ALREADY landed (ggen.toml + schema/domain.ttl, sync parses green). Declare packs via [[ontology.pack]]; never hand-edit a rendered consequence.
- Work ONLY in `/Users/sac/frozen-duckdb-wt/c4` on `feat/155-c4` (`/Users/sac/frozen-duckdb-wt/c4` paths are absolute above; the marketplace lives at ~/ggen-marketplace, READ-ONLY for you unless your ticket says otherwise). Never push. Atomic conventional commits.
- `ggen sync run` must exit 0 after your changes; `cargo build --workspace` must stay green; every render/gate EXECUTED once with command+exit preserved (証).
- Append transitions to the History table below (sanctioned main-checkout write), then STOP — the coordinator merges.

## History

| ts | standing | branch+SHA | gates+exits | remaining |
|----|----------|------------|-------------|-----------|
| 2026-09-21 | OPEN | feat/155-c4@304a958 | not started | all of DoD |
| 2026-09-21 | PARTIAL_ALIVE | feat/155-c4@f2fa14b | 階: pack read fully (pack.toml+ontology.ttl+9 templates). failed(edge_1): `[[ontology.pack]]` (ticket contract form, also the init scaffold's own commented example) REJECTED by ggen 26.8.18 — `unknown field 'pack', expected one of source/imports/base_iri/prefixes/standard_only` [FM-CONFIG-003]. failed(edge_2): `[packs]` table → FM-CONFIG-101 ambiguous-schema (frontmatter marker only, not for declarative consumers). failed(edge_3): `ggen pack add` → not_found (legacy flat ~/.ggen/packs registry, not marketplace layout). OPERATIVE form found & verified: `ontology.imports = ["/Users/sac/ggen-marketplace/packs/dry-run-publish-pack/ontology.ttl"]` — pack ontology in sync closure, content hash d37ed1c8…. Tilde NOT expanded (absolute path required). `rules = []` conflicts with `[[generation.rules]]` (duplicate key). Probes run in /tmp scratch, then removed | render + verify remaining |
| 2026-09-21 | PARTIAL_ALIVE | feat/155-c4@f2fa14b | ggen.toml: pack declared via ontology.imports + 3 [[generation.rules]] (matrix / domain / shapes). schema/domain.ttl +133: fd:publishGateBindings (6 phases × {runbookSteps, evidenceCommand, PDDL atom/action chain}), fd:gateObject RC-FROZEN-DUCKDB-V155, PKG-FROZEN-DUCKDB-{BUILDER,SYS,""} publish set. gates/ rendered (282 lines): dry-run-publish-gates.md (matrix + fence), dry-run-publish/dry-run-publish-domain.ttl (6 PDDL8 fragments + merged problem, init scope-engaged, sole goal dry-run-verified), dry-run-publish/dry-run-publish-shapes.ttl. 証: `ggen sync run` exit 0; second pass all three "skipped: unchanged: content identical" (graph 900d5861d515001f); signed ggen-receipt/v2 andon=Green (chain 1097cf59…); fence tripwire `! grep -qE '\b(published\|crates-io-uploaded\|release-complete)\b' gates/dry-run-publish/dry-run-publish-domain.ttl && grep -q dry-run-verified …` exit 0 (terminal atom ×5); rdflib parse: domain 21 triples, shapes 43 triples, both OK. Ledger: (a) ggen sync writes `.ggen/keys/signing.key` (PRIVATE) + `.ggen-v2/receipt*` into cwd — NOT committed (secrets law); repo .gitignore row for `.ggen*/` needed from a ticket that owns it (UNSUPPORTED here, scope); (b) runbook alignment section hand-authored (43 lines, below) — sanctioned by C4 scope, UNSUPPORTED(ggen rule render for docs/sjira prose) until a docs render rule exists | runbook alignment + receipt |
| 2026-09-21 | ALIVE | feat/155-c4@f1387f4 | PUBLISH_RUNBOOK.md: "Modeled dry-run gate (dry-run-publish-pack — 6 phases)" section between pre-flight verdict and cut sequence (artifact index, fence wording visible verbatim, phase↔step mapping, tripwire). 証: reconciliation grep — matrix step refs vs runbook: steps 1-11 all `^### n\.` OK + Pre-flight verdict OK + Abort doctrine OK (run pre- AND post-edit); post-edit re-render idempotent (written=[], all unchanged); `cargo build --workspace` exit 0 (post-change, 0.42s, prebuilt cache). DoD: pack declared ✅ / 6-phase alignment rendered+reconciled ✅ / fence law in rendered artifact ✅ / sync exit 0 ✅ / committed ✅. 比 (産面): manufactured 282 / delivered 325 = 87% (gates/ rendered by ggen vs 43 hand-authored runbook doc lines); source-side authorship excluded per 源 (schema+ggen.toml = render sources, like pack authorship). Strict denominator incl. sources: 282/698 = 40% — grows as gates/ re-renders; paydown = future gate phases ride the rules. Operator did NOT write: ggen.toml rules, schema bindings, all 282 gates/ lines. Falsifiers attempted: banned-atom grep (0 hits), RDF parse, two-pass byte-compare, reconciliation loop, template-variable trap (r.entry loop.first/last bug caught+fixed pre-commit). STOP — coordinator merges | none — ticket complete |
| 2026-09-22T00:50Z | ALIVE | feat/duckdb-1.5.5 (integration dry run, post-merge 30b14fc) | Post-merge repair of this ticket's surface: the coordinator's domain.ttl union DROPPED the `fd:project` fact block (comment header survived, facts did not; block present at f1387f4 line 9, absent at 30b14fc) → shapes-rule query returned ZERO rows → ggen's undocumented single-row context flattening yielded no `v`/`rc` → `[FM-GEN-008] TEMPLATE_VARIABLE_MISSING variable 'v'`. Fix: (a) `fd:project` restored byte-identical to f1387f4 in schema/domain.ttl (the C4 rules' natural key); (b) shapes template hardened to `{% for r in results %}` (`r.v`/`r.rc`), the form all 10 sibling rules use — no bare-variable flattening dependency; render re-verified byte-identical to the committed C4 render (f2fa14b). 証: `ggen sync run` exit 0 twice, second pass `"written": []`; full dry-run battery ALIVE (MILESTONE.md "Wave 4 dry run"); fence tripwire exit 0 | none — still complete; union repaired |
