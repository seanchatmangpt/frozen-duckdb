---
id: G6
dcterms:title: "in-code doc-comment crawl"
dcterms:created: "2026-09-21"
dcterms:isPartOf: "v26.9.21"
rdf:type: oslc_cm:ChangeRequest, earl:TestRequirement
aps:standing: https://w3id.org/chatman/aps#OPEN
milestone: frozen-duckdb v1.5.5 wave 5 (gap/staleness crawl → pack feedback)
worktree: /Users/sac/frozen-duckdb-wt/g6
branch: feat/155-g6
---

# G6 — in-code doc-comment crawl

## Mission

Falsify every claim in your slice against current reality. Outdated → fix at the
lawful source (pack facts → re-render for consequences; doc text in place for
prose). Missing → add or ledger. Feed pack-relevant findings into
docs/sjira/v26.9.21/PACK_FEEDBACK.md (append your section; create with header if
absent). 偽: a claim you did not test is UNKNOWN, not true.

## Scope

```
crates/**{Cargo.toml,*.rs} doc comments only
```

## Contract (all tickets)

- Base f3120fd: all gates green (ggen sync run, make verify, make genesis-check, 301 tests). Keep them green.
- Work ONLY in `/Users/sac/frozen-duckdb-wt/g6` on `feat/155-g6` (G9: marketplace worktree per ticket). Never push. Atomic conventional commits.
- Rendered consequences are edited at their source + re-rendered via `ggen sync run` — never hand-edited (exceptions must be ledgered UNSUPPORTED with the reason).
- Preserve command+exit evidence. Append transitions to the History table below (sanctioned main-checkout write), then STOP — the coordinator merges.

## History

| ts | standing | branch+SHA | gates+exits | remaining |
|----|----------|------------|-------------|-----------|
| 2026-09-21 | OPEN | feat/155-g6@f3120fd | not started | all of DoD |
| 2026-09-22T03:55Z | ALIVE | feat/155-g6@3f9d7c8 (+008b1f9, 869a206) | baseline `cargo test --workspace` exit 0 = 301/0 across 17 suites; post-edit rerun exit 0 = 301/0, 17 suites (unchanged); `cargo build` exit 0; git diff mechanically verified comment-lines-only. Falsifiers: crates.io API (duckdb 1.4.0 exists 2025-09-17 legacy series; encoded 1.10500.0..1.10505.0 real; law 1.MAJ*10000+MIN*100+PATCH corroborated, 1.10505.0=DuckDB 1.5.5); GitHub API releases=[] tags=[], v1.5.5 asset URL 404; measured dylib sizes 117,005,184 B BOTH archs (docs said 50/55MB); CLI probes on all 12 subcommands (convert + summarize panic exit 101 in clap debug asserts on short-flag collisions -i/-o and -m; embed/search "not implemented" panics exit 101; complete clean exit 1; info/test/benchmark silent at default WARN verbosity; tpch sf=0.01 = 86,805 data rows, lineitem 60,175, orders 15,000); sys build.rs rpath counterfactual (line removed → sys lib unittest SIGABRT in loader → reverted exactly → 89/0 green) | none — awaiting coordinator merge. Findings in PACK_FEEDBACK.md §G6. Behavior defects recorded NOT fixed (doc-only scope): clap short collisions, embed/search stub panics, exit codes 2/3 unimplemented (doc corrected) |
