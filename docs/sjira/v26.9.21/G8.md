---
id: G8
dcterms:title: "release docs coherence crawl"
dcterms:created: "2026-09-21"
dcterms:isPartOf: "v26.9.21"
rdf:type: oslc_cm:ChangeRequest, earl:TestRequirement
aps:standing: https://w3id.org/chatman/aps#ALIVE
milestone: frozen-duckdb v1.5.5 wave 5 (gap/staleness crawl → pack feedback)
worktree: /Users/sac/frozen-duckdb-wt/g8
branch: feat/155-g8
---

# G8 — release docs coherence crawl

## Mission

Falsify every claim in your slice against current reality. Outdated → fix at the
lawful source (pack facts → re-render for consequences; doc text in place for
prose). Missing → add or ledger. Feed pack-relevant findings into
docs/sjira/v26.9.21/PACK_FEEDBACK.md (append your section; create with header if
absent). 偽: a claim you did not test is UNKNOWN, not true.

## Scope

```
docs/sjira/v26.9.21/{PUBLISH_RUNBOOK,MILESTONE,PR_BODY,ISSUE_1_COMMENT,TPUB}.md
```

## Contract (all tickets)

- Base f3120fd: all gates green (ggen sync run, make verify, make genesis-check, 301 tests). Keep them green.
- Work ONLY in `/Users/sac/frozen-duckdb-wt/g8` on `feat/155-g8` (G9: marketplace worktree per ticket). Never push. Atomic conventional commits.
- Rendered consequences are edited at their source + re-rendered via `ggen sync run` — never hand-edited (exceptions must be ledgered UNSUPPORTED with the reason).
- Preserve command+exit evidence. Append transitions to the History table below (sanctioned main-checkout write), then STOP — the coordinator merges.

## History

| ts | standing | branch+SHA | gates+exits | remaining |
|----|----------|------------|-------------|-----------|
| 2026-09-21 | OPEN | feat/155-g8@f3120fd | not started | all of DoD |
| 2026-09-22 | ALIVE | feat/155-g8@50b93cd (base 63070af; commits bff6e17 evidence-refresh + 50b93cd crawl) | FALSIFIED FIRST (all by execution/gh this session): PR #3 MERGED a3a69e4 2026-09-22T00:59:40Z (`gh pr view 3` state=MERGED); c7b95ce is the PR-branch tip INSIDE the merge (not post-merge; only post-merge master commit is 63070af, docs-only); v1.5.5 absent local+remote (`git tag -l 'v1.5*'` empty, `git ls-remote --tags origin` empty); issue #1 CLOSED (auto via "closes #1" merge). CONTRADICTIONS FIXED (prose in place per ticket scope; 帳: no ggen-owned render touched — `ggen sync run` 11/11 unchanged): (1) PUBLISH_RUNBOOK step 1 `# OPEN` → DONE with merge evidence; tag target pinned a3a69e4; (2) C4 section "cut sequence above" → below; C5 provenance gate folded into linear cut sequence (pre-cut repo law) + phase rows 2/6, phase mapping and step numbering kept (rendered gates matrix stays aligned); (3) step 10: issue-1 state `OPEN` → CLOSED + posting command now extracts body after `---` separator (was: whole file incl. meta header posted); (4) ISSUE_1_COMMENT binding fence added (post only after steps 4–8; "it shipped"/"is out" true only at post time; body verbatim); (5) PR_BODY marked historical (merged PR body verbatim; cited test heads historical); (6) MILESTONE merge checkbox flipped with evidence, tag + issue-comment cuts annotated, wave-5 section appended (merge event + crawl summary; History tables untouched); (7) PACK_FEEDBACK.md created (G8 section: 5 pack findings — hardcoded `(OPEN)` in rendered gates phase-1 row [owned upstream, NOT fixed], runbook manual 3-source composition, fence not propagating to comms artifacts, merge-event staleness structural, C5 gate placement). KNOWN-LEFT (owned upstream, recorded not fixed): rendered `gates/dry-run-publish-gates.md` phase-1 `(OPEN)` stale at merge — render source edit + re-render belongs to pack/schema slice. GATES (worktree, this session): `ggen sync run` ×2 exit 0, `"written": []` both passes, same graph_hash 6bba05e3…; `cargo build --workspace` exit 0; `make verify` exit 0 (receipt valid:true signature_valid:true, 11 outputs); `make genesis-check` exit 0 (checked=37 deferred=0 gaps=8 failed=0, gaps pre-existing documented); `cargo test --workspace` exit 0 (all suites green incl. lib 140, sys 89, dropin 7, frozen 8, flock 11, vss 8, tpch 6, parquet 6); C4 fence tripwire exit 0. No push, no tag, no publish (権). | coordinator: merge feat/155-g8; operator cuts per harmonized PUBLISH_RUNBOOK (tag a3a69e4 next) |
