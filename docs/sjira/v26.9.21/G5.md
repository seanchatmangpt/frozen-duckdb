---
id: G5
dcterms:title: "scripts/ crawl: headers + drift"
dcterms:created: "2026-09-21"
dcterms:isPartOf: "v26.9.21"
rdf:type: oslc_cm:ChangeRequest, earl:TestRequirement
aps:standing: https://w3id.org/chatman/aps#OPEN
milestone: frozen-duckdb v1.5.5 wave 5 (gap/staleness crawl → pack feedback)
worktree: /Users/sac/frozen-duckdb-wt/g5
branch: feat/155-g5
---

# G5 — scripts/ crawl: headers + drift

## Mission

Falsify every claim in your slice against current reality. Outdated → fix at the
lawful source (pack facts → re-render for consequences; doc text in place for
prose). Missing → add or ledger. Feed pack-relevant findings into
docs/sjira/v26.9.21/PACK_FEEDBACK.md (append your section; create with header if
absent). 偽: a claim you did not test is UNKNOWN, not true.

## Scope

```
scripts/**; scripts/README*.md
```

## Contract (all tickets)

- Base f3120fd: all gates green (ggen sync run, make verify, make genesis-check, 301 tests). Keep them green.
- Work ONLY in `/Users/sac/frozen-duckdb-wt/g5` on `feat/155-g5` (G9: marketplace worktree per ticket). Never push. Atomic conventional commits.
- Rendered consequences are edited at their source + re-rendered via `ggen sync run` — never hand-edited (exceptions must be ledgered UNSUPPORTED with the reason).
- Preserve command+exit evidence. Append transitions to the History table below (sanctioned main-checkout write), then STOP — the coordinator merges.

## History

| ts | standing | branch+SHA | gates+exits | remaining |
|----|----------|------------|-------------|-----------|
| 2026-09-21 | OPEN | feat/155-g5@f3120fd | not started | all of DoD |
| 2026-09-21 | ALIVE (DoD met on branch; STOP for coordinator merge) | feat/155-g5@7bd9183 (3 commits: 075d1d4 removals+manifest, dc70ea6 rg_sweep fix, 7bd9183 docs+PACK_FEEDBACK) | bash -n scripts/*.sh → 38 files, 0 failures (baseline had docs_check.sh parse-FAIL); make gates → exit 0 (ggen sync 0; genesis-check checked=36 gaps=1 failed=0, down from 37/8); cargo build --workspace → exit 0 (27.5s; log live-confirms central cache: ~/.frozen-duckdb/cache/v1.5.5-arm64/libduckdb_arm64.dylib); rg_sweep.sh live → exit 0. Live falsifiers this session: ./scripts/test_ffi_simple.sh → exit 1 "DuckDB library not found" (setup_env.sh `$0` → DUCKDB_LIB_DIR=…/scripts); ./scripts/run_ffi_validation.sh → exit 1 at Go leg (same mechanism); scan_fakes.sh → exit 2 (lib/config.sh:39 `declare -A` under stock bash 3.2.57); scan_fakes_core_team.sh → dies line 29 (same) + never sources lib though it calls get_script_config + exec bit 644 + stub body; cargo metadata --manifest-path scripts/Cargo.toml → exit 101 "no targets"; docs_check.sh → bash -n FAIL (line 94 backticks-in-quotes) + requires nonexistent mkdocs.yml/docs tree + dead kcura-cli build. Decisions (TR6 deferred triage, evidence per file): REMOVED scan_fakes.sh, scan_fakes_core_team.sh, lib/{config,intelligent_cache,logging,self_healing}.sh, kcura-config.yaml, kcura-config.example.yaml, redteam_probe.rs (orphaned non-target, mock probes), scripts/Cargo.toml (unbuildable), docs_check.sh (parse-rot); KEPT+FIXED rg_sweep.sh (kcura-path sections trimmed — crates/kcura-* never existed here; real frozen-duckdb-sys FFI probe added; G5 STATUS header); REWRITTEN scripts/README.md + scripts/README_gap_detection.md (KCura identity + removed-gate claims reconciled); CREATED docs/sjira/v26.9.21/PACK_FEEDBACK.md (6 pack proposals). Header-crawl verdicts on the 6 kept files: all TR6 STATUS claims verified accurate (central cache v1.4.0+v1.5.5 both arches present; $0 defect live; examples exist at crates/frozen-duckdb/examples/; CRATE_VERSION=0.1.0 stale vs real 1.5.5) — no header edits needed. Manifest/ledger in the same commits: GENESIS −8 rows (7 gap + lib/config.sh active) + G5 paydown note; HANDWRITTEN row 8 drops lib/config.sh, Known-drift → Resolved-paydown record — ledger SHRINKS monotonically. Remaining (T6 lane, PACK_FEEDBACK rows): FFI lib resolution repair + setup_env.sh $0→BASH_SOURCE, validate_prod_build.sh CRATE_VERSION bump, stale kcura strings in live scripts (census: 33 files cosmetic, 7 functional incl. spec_sync_check.sh → dead docs/specs/errors.yaml). Operator wrote: nothing — 3 agent commits, net 209 insertions / 3207 deletions. | none in DoD |
