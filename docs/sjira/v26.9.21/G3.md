---
id: G3
dcterms:title: "docs/ tree staleness crawl"
dcterms:created: "2026-09-21"
dcterms:isPartOf: "v26.9.21"
rdf:type: oslc_cm:ChangeRequest, earl:TestRequirement
aps:standing: https://w3id.org/chatman/aps#OPEN
milestone: frozen-duckdb v1.5.5 wave 5 (gap/staleness crawl → pack feedback)
worktree: /Users/sac/frozen-duckdb-wt/g3
branch: feat/155-g3
---

# G3 — docs/ tree staleness crawl

## Mission

Falsify every claim in your slice against current reality. Outdated → fix at the
lawful source (pack facts → re-render for consequences; doc text in place for
prose). Missing → add or ledger. Feed pack-relevant findings into
docs/sjira/v26.9.21/PACK_FEEDBACK.md (append your section; create with header if
absent). 偽: a claim you did not test is UNKNOWN, not true.

## Scope

```
docs/{architecture,guides,api,cli,contributing,testing}/**
```

## Contract (all tickets)

- Base f3120fd: all gates green (ggen sync run, make verify, make genesis-check, 301 tests). Keep them green.
- Work ONLY in `/Users/sac/frozen-duckdb-wt/g3` on `feat/155-g3` (G9: marketplace worktree per ticket). Never push. Atomic conventional commits.
- Rendered consequences are edited at their source + re-rendered via `ggen sync run` — never hand-edited (exceptions must be ledgered UNSUPPORTED with the reason).
- Preserve command+exit evidence. Append transitions to the History table below (sanctioned main-checkout write), then STOP — the coordinator merges.

## History

| ts | standing | branch+SHA | gates+exits | remaining |
|----|----------|------------|-------------|-----------|
| 2026-09-21 | OPEN | feat/155-g3@f3120fd | not started | all of DoD |
| 2026-09-22 | ALIVE | feat/155-g3@8cc2849 (7 commits over 63070af) | `ggen sync run` exit 0 ×2 (second pass byte-identical, no tree diff); `cargo build --workspace` exit 0; `cargo test --workspace` exit 0 — 18 suites, 303 passed, 0 failed (301 base + 2 new tripwires); all 12 `frozen-duckdb-cli <cmd> --help` exit 0 (`convert`/`summarize` exit 101 pre-repair — duplicate clap shorts, witnessed); dead-link sweep 0, referenced-path sweep 0 | coordinator merge only |

### G3 session record (2026-09-21/22)

- **Verdicts**: architecture ALIVE-after-fix (command list 9→12, exit codes, ADR 003 27% claim); guides ALIVE-after-fix (chinook duckdb no-op, TPC-H SF fixed 0.01, convert CSV↔Parquet only, env-var/custom-build.rs sections rewritten, 2 dead links); api ALIVE-after-fix (full option-table re-derivation from live `--help`, validate-ffi added, exit codes 0/1/2/4, flock 36% claim falsified 11/11); cli ALIVE-after-fix (same class + benchmark "coming soon" removed); contributing ALIVE-after-fix (27%×2, ADR 004 superseded, repo layout, ARCH test matrix, proptest marked illustrative); testing ALIVE-after-fix (run_ffi_validation.sh KEPT-UNCERTAIN/BLOCKED state surfaced in doc, 4 dead links, maintained path = `validate-ffi`).
- **Code repair (帳-ledgered)**: `crates/frozen-duckdb/src/cli/commands.rs` — Convert `--input-format`/`--output-format` and Summarize `--max-length` demoted to long-only (duplicate shorts `-i`/`-m` made clap debug asserts panic at parse, exit 101; `convert`/`summarize` could not run at all). Permanent guard: `crates/frozen-duckdb/tests/cli_surface_tests.rs` (debug_assert + help render, all subcommands). Row added to HANDWRITTEN.md (frozen-duckdb-cli pack — argument-surface family, unadmitted).
- **Pack feedback**: docs/sjira/v26.9.21/PACK_FEEDBACK.md created with rows G3-1..G3-7.
- **Out of slice (recorded, not touched)**: docs/llm/ binary-name drift (G3-6); stale runtime error strings in src/main.rs ("Run 'frozen-duckdb flock-setup' first") (G3-2).
- **Commits**: ac77ae3 fix(cli) · 39bfc21 docs(architecture,guides) · 9d3939b docs(api) · 108716b docs(cli) · 0b92e89 docs(contributing,testing) · 530d831 docs(sjira PACK_FEEDBACK) · 8cc2849 docs residual sweep. Never pushed.
