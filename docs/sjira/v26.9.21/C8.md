---
id: C8
dcterms:title: "verify-gate chain"
dcterms:created: "2026-09-21"
dcterms:isPartOf: "v26.9.21"
rdf:type: oslc_cm:ChangeRequest, earl:TestRequirement
aps:standing: https://w3id.org/chatman/aps#OPEN
milestone: frozen-duckdb v1.5.5 wave 4 (marketplace capability composition)
worktree: /Users/sac/frozen-duckdb-wt/c8
branch: feat/155-c8
---

# C8 — verify-gate chain

## 階 law (search ladder, before any write)

REUSE the pack as-is → COMPOSE packs → EXTEND a pack family → INVENT only as an
admitted pack. Record every failed edge in your ticket History: failed(edge) ≠
failed(G); silent pruning forbidden.

## Scope

```
ggen.toml; schema/; scripts/; Makefile (new)
```

Anything outside scope: do not edit — ledger it instead.

## Work order


Packs: packs/ggen-verify-pack and packs/evidence-standing-pack (assess, pick fit). Goal: one repo-side verify capability that chains the existing gates (cargo fmt/clippy/build/test, test-validation harness, ggen sync run, receipt verify) into a single declared, rendered target (Makefile verify) so the full gate battery is one command and its standing vocabulary (ALIVE/BLOCKED/…) is rendered from pack facts, not ad-hoc.
DoD: [ ] pack declared/assessed  [ ] verify target rendered and EXECUTED once end-to-end exit 0  [ ] sync exit 0  [ ] committed

## Contract (all tickets)

- ggen consumer bootstrap ALREADY landed (ggen.toml + schema/domain.ttl, sync parses green). Declare packs via [[ontology.pack]]; never hand-edit a rendered consequence.
- Work ONLY in `/Users/sac/frozen-duckdb-wt/c8` on `feat/155-c8` (`/Users/sac/frozen-duckdb-wt/c8` paths are absolute above; the marketplace lives at ~/ggen-marketplace, READ-ONLY for you unless your ticket says otherwise). Never push. Atomic conventional commits.
- `ggen sync run` must exit 0 after your changes; `cargo build --workspace` must stay green; every render/gate EXECUTED once with command+exit preserved (証).
- Append transitions to the History table below (sanctioned main-checkout write), then STOP — the coordinator merges.

## History

| ts | standing | branch+SHA | gates+exits | remaining |
|----|----------|------------|-------------|-----------|
| 2026-09-21 | OPEN | feat/155-c8@304a958 | not started | all of DoD |
| 2026-09-21 23:40 | OPEN | feat/155-c8@304a958 | 階 done. REUSE: ggen-verify-pack FIT (ver: vocabulary; ships ZERO RequiredCheck individuals by design — consumers author their own facts); evidence-standing-pack FIT for standing vocabulary (es:Standing individuals + es:standingName = the repo's ALIVE/BLOCKED/BUILD_BROKEN set, verbatim). FAILED EDGES recorded (silent pruning forbidden): (1) ticket-named syntax [[ontology.pack]] REFUSED by actuator ggen@26.8.18 — `[FM-CONFIG-003] unknown field 'pack'` (empirically tested in /tmp scratch; schema-true declarative form is [[packs]]); (2) [[packs]] is INERT as a pack loader on the declarative-rules schema — sync dispatches to generation_rules::run, which resolves [[packs]] only as a rule-local file-source registry (QuerySource/TemplateSource::Pack) and hardcodes report `packs: {}`; pack ontologies/gates/templates do not auto-bind (that is the frontmatter schema's `[packs.<name>]` behavior; `[packs.<name>]` here is refused as schema-ambiguous FM-CONFIG-101); (3) TemplateSource::Pack of pack templates unusable on this schema: pack .tmpl carry frontmatter `to:`/`sparql:` contracts and generation rules render template text raw (no frontmatter stripping) — failed(ggen-verify-pack emitter reuse as rule template); (4) declaring evidence-standing-pack as a full [[packs]] pack would render 4 out-of-C8-scope chain consequences (src/es/chain.rs/.py/.ts, lib/es/chain.ex from its chain.*.tmpl) → COMPOSED via [ontology].imports instead (its ontology facts only, closure-hashed into every receipt); (5) verify-pack SPARQL gates 010/020/030/040/050 auto-run only on the frontmatter schema — not bound here; battery judgment rendered into the gate runner + make exit propagation instead (recorded, not silently dropped); (6) ggen sync run / ggen receipt verify deliberately NOT declared as ver:RequiredCheck facts — a check whose command runs the actuator that renders the check's own harness is the self-reference deadlock class verify-pack's kd-byte-identity documents; they are chained one level up in the rendered Makefile verify target | author facts + rules, render, execute |
| 2026-09-21 23:55 | ALIVE | feat/155-c8@648600f | Authored schema/verify.ttl (5 ver:RequiredCheck: fmt-check, clippy-deny-warnings, build-workspace, test-workspace, test-validation; fdv:redStanding → es: individuals) + schema/verify-gates.rq (one UNION row source: checks + es:Standing vocabulary rows) + ggen.toml ([ontology].imports binding both packs; rules render-verify-gates + render-makefile-verify, mode=Overwrite; Makefile claims `verify`+`verify-*` ONLY — `sync`/`gates` left to C10 per its ticket). RENDERED by `ggen sync run` exit 0 (written: Makefile, scripts/verify-gates.sh; both pack ontologies closure-hashed in receipt; one render iteration fixed: Tera `{#` comment-open vs bash `${#...}` → {% raw %} wrap). EXECUTED `make verify` once end-to-end: MAKE_VERIFY_EXIT=0 — 5/5 gates green (build-workspace exit 0, 38.75s; clippy-deny-warnings exit 0 -D warnings, 16.71s; fmt-check exit 0; test-validation exit 0 "ALL ASSERTIONS HELD"; test-workspace exit 0), STANDING: ALIVE, inner `ggen sync run` idempotent ("unchanged: content identical" ×2), `ggen receipt verify` valid=true signed=true (chain ab22af19…, graph 8a34acde…). Evidence committed: scripts/verify-evidence.ttl = 5 ver:Check facts, all exitCode 0, ver:verifiedGraphHash 8a34acde…ddd1. POST-COMMIT re-check @648600f: `ggen sync run` exit 0, `ggen receipt verify` exit 0 (chain 19222f82…). Commits: f50232b feat(verify) facts+rules; 648600f chore(verify) renders. 比: consequence lines 158/158 manufactured (Makefile 43 + runner 80 + evidence 35 all rendered by ggen sync from facts; 0 hand-written consequence lines); hand-authored lines are policy/ontology/query/manifest declarations — the authorship path ggen-verify-pack v26.7.20 itself assigns to consumers. Operator wrote: none of the battery, none of the renders. DoD: pack assessed+bound ✅ / verify target rendered+EXECUTED exit 0 ✅ / sync exit 0 ✅ / committed ✅. C10 collision note: C8 owns `verify` and the `verify-*` namespace; C10 keeps `sync`+`gates` per its own ticket clause — no overlap at this base | none — STOP, coordinator merges |
