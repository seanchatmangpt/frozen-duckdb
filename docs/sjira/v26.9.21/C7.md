---
id: C7
dcterms:title: "castle-changelog-release-pack"
dcterms:created: "2026-09-21"
dcterms:isPartOf: "v26.9.21"
rdf:type: oslc_cm:ChangeRequest, earl:TestRequirement
aps:standing: https://w3id.org/chatman/aps#OPEN
milestone: frozen-duckdb v1.5.5 wave 4 (marketplace capability composition)
worktree: /Users/sac/frozen-duckdb-wt/c7
branch: feat/155-c7
---

# C7 — castle-changelog-release-pack

## 階 law (search ladder, before any write)

REUSE the pack as-is → COMPOSE packs → EXTEND a pack family → INVENT only as an
admitted pack. Record every failed edge in your ticket History: failed(edge) ≠
failed(G); silent pruning forbidden.

## Scope

```
ggen.toml; schema/; CHANGELOG.md
```

Anything outside scope: do not edit — ledger it instead.

## Work order


Pack: packs/castle-changelog-release-pack. Assess fit against the hand-maintained CHANGELOG.md (a hand-written surface = 帳 debt). If the pack renders changelogs from facts (git history/tags/ontology), project v1.5.5's entry and reconcile; if it doesn't fit (wrong domain, needs unavailable inputs), that is a FAILED EDGE to record in detail — failed(edge) ≠ failed(G), silent pruning forbidden. Either way the outcome is a rendered artifact or a ledgered edge, never a silent skip.
DoD: [ ] pack assessed with evidence  [ ] rendered or failed-edge ledgered  [ ] sync exit 0  [ ] committed

## Contract (all tickets)

- ggen consumer bootstrap ALREADY landed (ggen.toml + schema/domain.ttl, sync parses green). Declare packs via [[ontology.pack]]; never hand-edit a rendered consequence.
- Work ONLY in `/Users/sac/frozen-duckdb-wt/c7` on `feat/155-c7` (`/Users/sac/frozen-duckdb-wt/c7` paths are absolute above; the marketplace lives at ~/ggen-marketplace, READ-ONLY for you unless your ticket says otherwise). Never push. Atomic conventional commits.
- `ggen sync run` must exit 0 after your changes; `cargo build --workspace` must stay green; every render/gate EXECUTED once with command+exit preserved (証).
- Append transitions to the History table below (sanctioned main-checkout write), then STOP — the coordinator merges.

## History

| ts | standing | branch+SHA | gates+exits | remaining |
|----|----------|------------|-------------|-----------|
| 2026-09-21 | OPEN | feat/155-c7@304a958 | not started | all of DoD |
| 2026-09-21 | PARTIAL_ALIVE | feat/155-c7@304a958 | pack assessed with evidence: read all 4 pack files (pack.toml, ontology.ttl, templates/CHANGELOG.md.tmpl, templates/release.yml.tmpl; no gates/, no README). VERDICT: pack FITS this repo's CHANGELOG domain — schema-only pack (zero individuals) that renders Keep-a-Changelog CHANGELOG.md by SPARQL over consumer-authored rel:Release/rel:ChangeEntry facts unioned against its closed six-category SKOS scheme (public-ontology-first: dcterms/skos, only rel:Release+rel:ChangeEntry minted). FAILED EDGES vs ggen@26.8.18 (binary-probed via strings + sync probes): (E1) the `[[ontology.pack]]` key named by the bootstrap comment does not exist — OntologyConfig accepts only source/imports/base_iri/prefixes/standard_only (`FM-CONFIG-003 unknown field pack`); lawful substitute = `[ontology].imports` union + top-level `[[packs]]` (PackRef name/version/path) declaration; (E2) pack templates speak the frontmatter dialect (to:+named sparql:) but the declarative `[[generation.rules]]` pipeline (what this repo's bootstrap landed on) renders rule templates as plain Tera with the single rule query bound to `results`/`sparql_results` and does NOT parse template frontmatter — pack resolution (TemplateSource::Pack) is typed against the other (frontmatter) ggen.toml schema only ("skipped: pack resolution is not implemented for the declarative-rules schema yet"); consumers may not restructure the shared ggen.toml to the frontmatter schema (sibling C-tickets append [[generation.rules]]); (E3) `ggen pack add` expects a flat `<name>.toml` registry layout, not the marketplace's directory packs (status not_found); (E4) templates/release.yml.tmpl NOT rendered: outside ticket scope (ggen.toml; schema/; CHANGELOG.md), 権 (workflow/tag mutation = coordinator/operator surface per RUNBOOK), and the pack's own header says a github-actions-pack composition is preferred — left as a ledgered edge, not silently skipped | render, reconcile, gates, commit |
| 2026-09-21 | ALIVE | feat/155-c7@a09ce8a | RENDERED: v1.5.5 CHANGELOG.md projected from facts. (1) Facts authored at the pack's designed surface: schema/changelog.ttl = fd:release-v1.5.5 (rel:version 1.5.5, rel:date 2026-09-21) + 14 rel:ChangeEntry individuals with rel:category from the pack's closed scheme + explicit rel:entryOrder; reconciliation of the hand-written file is fact-preserving and documented in TTL comments: 12/15 bullets verbatim; "Fixes and hardening (wave 3)" (consumer-minted 7th category, forbidden by the closed scheme) re-categorized into rel:catFixed (6 bullets); release-premise facts fold into the pin-bump Changed entry (upstream v1.5.5 released 2026-07-22; crate-version-tracks-bundled-DuckDB — convention's lawful home = schema/domain.ttl fd:duckdbVersionLaw); "Known limitations" facts fold into the Linux/multi-arch Fixed entry (macOS-only universal arm64+x86_64 binaries; Linux/Windows on local-compile fallback; prebuilt .so planned) — all 4 folded fact-strings verified present in the render. (2) Render wiring: [ontology].imports = pack ontology.ttl + schema/changelog.ttl; [[generation.rules]] name=changelog, one deterministic UNION SELECT (ORDER BY ?kind DESC(?date) DESC(?version) ?rel ?category ?entryOrder, strict_mode E0013 satisfied) binding `results`; templates/CHANGELOG.md.tmpl = consumer ADAPTER whose diff vs the pack template is exactly two set lines deriving the pack's named `releases`/`entries` from `results`, pack body verbatim — 帳 ledgered as UNSUPPORTED(castle-changelog-release-pack frontmatter-template render in declarative schema), owner pack/ggen-engine, paydown = repoint template.file at the pack template when ggen ships pack resolution or frontmatter parsing for declarative rules. (3) GATES: `ggen sync run` exit 0 wrote CHANGELOG.md (closure receipt hashes pack ontology ea4301d4, schema/changelog.ttl e32b60dc, domain.ttl df8037f0, adapter 551e12d1); re-run exit 0 "unchanged: content identical" (determinism, SHA c4515f56 stable); `ggen receipt verify` exit 0 valid:true signed:true signature_valid:true chain e9d6f7cd; `cargo build --workspace` exit 0; fact-preservation diff vs HEAD:CHANGELOG.md = 12 verbatim / 2 folds / 0 fact losses. (4) 帳 PAYDOWN: CHANGELOG.md ceases to be hand-maintained debt — it is now a generator-owned consequence (edit schema/changelog.ttl + re-sync; hand edits frozen out by sync). NOT DONE (ledgered, not silently skipped): .ggen-v2/ + .ggen/ runtime receipts untracked and .gitignore lacks ggen entries — .gitignore outside this ticket's scope, coordinator to own or assign; release.yml edge E4 stands | none for C7; coordinator merges feat/155-c7 |
