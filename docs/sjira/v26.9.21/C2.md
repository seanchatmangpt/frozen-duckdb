---
id: C2
dcterms:title: "readme-diataxis-pack composition"
dcterms:created: "2026-09-21"
dcterms:isPartOf: "v26.9.21"
rdf:type: oslc_cm:ChangeRequest, earl:TestRequirement
aps:standing: https://w3id.org/chatman/aps#OPEN
milestone: frozen-duckdb v1.5.5 wave 4 (marketplace capability composition)
worktree: /Users/sac/frozen-duckdb-wt/c2
branch: feat/155-c2
---

# C2 — readme-diataxis-pack composition

## 階 law (search ladder, before any write)

REUSE the pack as-is → COMPOSE packs → EXTEND a pack family → INVENT only as an
admitted pack. Record every failed edge in your ticket History: failed(edge) ≠
failed(G); silent pruning forbidden.

## Scope

```
ggen.toml; schema/; docs/**; README.md (read-mostly)
```

Anything outside scope: do not edit — ledger it instead.

## Work order


Pack: packs/readme-diataxis-pack (projects README facts into Diataxis quadrants: tutorials/getting-started.md, how-to/*, reference/*, explanation/*, docs/index.md, docs/meta.md). 階 first: read the pack's README.md + pack.toml + templates/ + gates/ in ~/ggen-marketplace. Declare it via [[ontology.pack]] in ggen.toml, seed consumer facts from the existing README, ggen sync run, reconcile rendered docs with existing docs/ (existing files that duplicate a quadrant get superseded or cross-linked, not silently deleted). Record failed edges where the pack's assumptions don't fit this repo.
DoD: [ ] pack declared + sync run exit 0  [ ] Diataxis set rendered or failed-edge ledgered  [ ] cargo gate still green  [ ] committed

## Contract (all tickets)

- ggen consumer bootstrap ALREADY landed (ggen.toml + schema/domain.ttl, sync parses green). Declare packs via [[ontology.pack]]; never hand-edit a rendered consequence.
- Work ONLY in `/Users/sac/frozen-duckdb-wt/c2` on `feat/155-c2` (`/Users/sac/frozen-duckdb-wt/c2` paths are absolute above; the marketplace lives at ~/ggen-marketplace, READ-ONLY for you unless your ticket says otherwise). Never push. Atomic conventional commits.
- `ggen sync run` must exit 0 after your changes; `cargo build --workspace` must stay green; every render/gate EXECUTED once with command+exit preserved (証).
- Append transitions to the History table below (sanctioned main-checkout write), then STOP — the coordinator merges.

## History

| ts | standing | branch+SHA | gates+exits | remaining |
|----|----------|------------|-------------|-----------|
| 2026-09-21 | OPEN | feat/155-c2@304a958 | not started | all of DoD |
| 2026-09-21 | ALIVE | feat/155-c2@7f604e5 | `ggen sync run` exit 0 — 15 Diataxis docs rendered (tutorials/getting-started, how-to/×5, reference/×4, explanation/×3, index, meta); replay exit 0, 15/15 `unchanged: content identical`; pack gates 010+020 sync-enforced — falsifier (duplicate-position RunStep) refused `[FM-PACK-013]` exit 1, restored; `cargo build --workspace` exit 0 (31.4s, prebuilt arm64 dylib) | none — DoD complete |

**Failed edges (recorded, none silent):**
1. `[[ontology.pack]]` (ticket/bootstrap-declared form) → ggen 26.8.18 refuses `[FM-CONFIG-003] unknown field \`pack\`` — the manifest schema's OntologyConfig admits only source/imports/base_iri/prefixes/standard_only. failed(edge) ≠ failed(G).
2. Declarative schema ([[generation.rules]]) cannot render this pack: its rules inject only `results`/first-row context; pack templates need named multi-SELECT result sets (steps/endpoints/...). Pack targets the frontmatter schema — switched ggen.toml to it ([packs.<name>] table-of-tables, the marketplace convention), which also auto-enforces pack gates.
3. `templates/` dir required (`[FM-CONFIG-004]` if missing on fresh checkout) → tracked one-byte `templates/.gitkeep` placeholder (帳: hand-placed byte outside generator path, no semantic content).
4. rdx:ApiEndpoint/ApiParameter unseeded — pack models an HTTP API; repo's surface is the duckdb-rs-mirror Rust API → docs/reference/api.md renders header-only tables.
5. rdx:ContactChannel unseeded — pack admits kind ∈ {email,slack} only; repo support is GitHub → meta.md Support section empty; contributing cross-linked via rdx:RelatedLink instead. meta.md Contributing boilerplate (`contributing.md`, code of conduct) is pack-template constant text; real docs/contributing/ retained untouched.
6. rdx:CodeExample/ExampleFile/PlaygroundFile unseeded — fan-outs would write examples/ + playground/, outside ticket scope (docs/**) → 0-row skips recorded in sync receipt; index.md Examples section keeps the template's hardcoded Playground nav line pointing at the nonexistent playground/.
7. ggen.toml/ggen.lock carry the absolute marketplace path (/Users/sac/ggen-marketplace/...) — machine-local but depth-stable across checkouts; a ../-relative path breaks at main-repo depth.

**比 note:** rendered docs = pack projection from seeded RDF; consumer-authored bytes this change = ggen.toml schema switch + RDF facts + .gitkeep (all in pack-declared config/ontology surfaces, the generator's own inputs). Existing docs/ tree (api/ architecture/ cli/ contributing/ guides/ llm/ testing/ sjira/) untouched — rendered paths are disjoint; reconciliation = cross-links inside the facts (rdx:RelatedLink → coding-standards), zero deletions. `.ggen/` (signing keys) left untracked — secrets never enter version control. Not pushed; coordinator merges.
