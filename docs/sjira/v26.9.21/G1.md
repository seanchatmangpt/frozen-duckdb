---
id: G1
dcterms:title: "Diataxis renders staleness crawl"
dcterms:created: "2026-09-21"
dcterms:isPartOf: "v26.9.21"
rdf:type: oslc_cm:ChangeRequest, earl:TestRequirement
aps:standing: https://w3id.org/chatman/aps#OPEN
milestone: frozen-duckdb v1.5.5 wave 5 (gap/staleness crawl → pack feedback)
worktree: /Users/sac/frozen-duckdb-wt/g1
branch: feat/155-g1
---

# G1 — Diataxis renders staleness crawl

## Mission

Falsify every claim in your slice against current reality. Outdated → fix at the
lawful source (pack facts → re-render for consequences; doc text in place for
prose). Missing → add or ledger. Feed pack-relevant findings into
docs/sjira/v26.9.21/PACK_FEEDBACK.md (append your section; create with header if
absent). 偽: a claim you did not test is UNKNOWN, not true.

## Scope

```
docs/{tutorials,how-to,reference,explanation}/**; docs/{index,meta}.md
```

## Contract (all tickets)

- Base f3120fd: all gates green (ggen sync run, make verify, make genesis-check, 301 tests). Keep them green.
- Work ONLY in `/Users/sac/frozen-duckdb-wt/g1` on `feat/155-g1` (G9: marketplace worktree per ticket). Never push. Atomic conventional commits.
- Rendered consequences are edited at their source + re-rendered via `ggen sync run` — never hand-edited (exceptions must be ledgered UNSUPPORTED with the reason).
- Preserve command+exit evidence. Append transitions to the History table below (sanctioned main-checkout write), then STOP — the coordinator merges.

## History

| ts | standing | branch+SHA | gates+exits | remaining |
|----|----------|------------|-------------|-----------|
| 2026-09-21 | OPEN | feat/155-g1@f3120fd | not started | all of DoD |
| 2026-09-21 | ALIVE | feat/155-g1@1860d11 (031597c fix(cli), 5e933a3 docs, 1860d11 chore(verify); base 63070af incl. merged PR #3) | ggen sync run 0; cargo build --workspace 0; cargo test --workspace 301/301 exit 0 (dev + release); dropin_replacement example 0; tpch download 0; convert 0 post-fix (panicked pre-fix: clap dup shorts -i/-o; summarize -m — fixed 031597c); 12-subcommand sweep panic-free; cargo doc 0; make verify 0 (gates+sync+receipt, signature_valid); genesis-check 0 checked=37 failed=0 | coordinator merge. Notes: corrections hand-applied in place (render path inoperative, FM-CONFIG-003), ledgered UNSUPPORTED in HANDWRITTEN.md (ledger grows 1 row + header exception sentence; paydown = re-render facts staged in PACK_FEEDBACK.md § G1). Out-of-slice: crates/frozen-duckdb/src/cli/commands.rs dup-short fix (no doc-side workaround existed — recipe aborted at registration). Unknowns: download sizes (zero releases, gh verified); flock-setup flags verified via registration only (not executed — mutates Ollama config); upstream DuckDB v1.5.5 release URL not re-fetched. README fact-source defects (universal-asset claim, extension list) belong to README owner — see PACK_FEEDBACK.md § G1 findings 1-3. HANDWRITTEN.md header sentence updated (contradicted own ledger post-C2 merge). |
