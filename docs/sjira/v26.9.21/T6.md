---
id: T6
dcterms:title: "Scripts repair: fiction + float pins"
dcterms:created: "2026-09-21"
dcterms:isPartOf: "v26.9.21"
rdf:type: oslc_cm:ChangeRequest, earl:TestRequirement
aps:standing: https://w3id.org/chatman/aps#OPEN
milestone: frozen-duckdb v1.5.5 (DuckDB 1.5.5 support, closes seanchatmangpt/frozen-duckdb#1)
worktree: /Users/sac/frozen-duckdb-wt/scripts-fix
branch: feat/155-scripts-fix
---

# T6 — Scripts repair: fiction + float pins

## Scope (files you may change)

```
scripts/**.sh
```

Anything outside scope: do not edit — record BLOCKED with the reason instead.

## Work order


Context: create_frozen_setup.sh fetches libduckdb_{arch}.dylib assets that never existed upstream (fictional scheme) — rewrite to fetch from this repo's GitHub Releases (the real post-fix scheme) or remove the dead path with an UNSUPPORTED note. build_frozen_duckdb.sh clones duckdb-rs default branch unpinned → pin --branch v1.10505.0 (duckdb-rs crate version encoding DuckDB 1.5.5). Sweep remaining 1.4.x pins (create_frozen_setup.sh has v1.4.1). bash -n everything touched. Do NOT execute build scripts.
DoD: [ ] grep -rn 'v1\.4' scripts/ → zero live pins  [ ] bash -n on all edited scripts → exit 0  [ ] committed

## Contract (all tickets)

- Work ONLY in `/Users/sac/frozen-duckdb-wt/scripts-fix` on `feat/155-scripts-fix`. NEVER touch the main checkout /Users/sac/frozen-duckdb or sibling worktrees. Never push, never touch remotes.
- Atomic commits, conventional messages (`test:`, `fix:`, `docs:`, `ci:`, `build:`), on your branch only.
- 流: read this ticket → 階 (reuse what exists) → edit → verify (run the DoD gates) → 証 (append History) → stop.
- Standing vocabulary: ALIVE | BLOCKED | BUILD_BROKEN | PARTIAL_ALIVE | REFUSED_* | UNKNOWN | UNSUPPORTED. Preserve every command + exit code. inspection ≠ execution: a gate you did not run is not green.
- Shared resources: ~/.frozen-duckdb/cache (read-mostly; T7 owns its v1.5.5-arm64 mutation), crates.io registry, network. Coordinate through the ticket, never by touching sibling trees.
- Update the History table on every transition, then STOP — the coordinator merges.

## History

| ts | standing | branch+SHA | gates+exits | remaining |
|----|----------|------------|-------------|-----------|
| 2026-09-21 | OPEN | feat/155-scripts-fix@c8e5788 | not started | all of DoD |
| 2026-09-21T21:08Z | ALIVE (DoD gates observed; asset 404 residual is expected pre-T8) | feat/155-scripts-fix@0adcb14+c4dc8b8 | bash -n scripts/{create_frozen_setup,build_frozen_duckdb,build_static_duckdb}.sh → exit 0 ×3; `grep -rn 'v1\.4' scripts/` → 0 live pins (2 residual matches are ticket prose inside scripts/gen_sjira_tickets.sh:36,40 — work-order text, not pins, left intact); `git ls-remote --tags duckdb-rs` → v1.10505.0 present (88f673c); HEAD `.../releases/download/v1.5.5/libduckdb_{arm64,x86_64}.dylib` → 404 (repo has zero published releases yet; T8 cuts v1.5.5, scheme matches builder lib.rs:252 + build-binaries.yml:81-101,135-136) | UNSUPPORTED ledger (帳): hand-edited scripts/create_frozen_setup.sh (fictional duckdb/duckdb v1.4.1 libduckdb_{arch}.dylib URL → seanchatmangpt/frozen-duckdb v1.5.5 scheme; README Option 2 clone pinned --depth 1 --branch v1.5.5, matching builder), scripts/build_frozen_duckdb.sh + scripts/build_static_duckdb.sh (unpinned duckdb-rs clones → --branch v1.10505.0; static twin is same-family float pin, ticket-title scope). No pack/generator expresses script fetch-scheme repair. Residual UNKNOWN: build scripts not executed (forbidden by ticket) — runtime behavior unexercised; new URLs verify 200 only after T8 publishes |
