---
id: G7
dcterms:title: "workflows + projection docs crawl"
dcterms:created: "2026-09-21"
dcterms:isPartOf: "v26.9.21"
rdf:type: oslc_cm:ChangeRequest, earl:TestRequirement
aps:standing: https://w3id.org/chatman/aps#ALIVE
milestone: frozen-duckdb v1.5.5 wave 5 (gap/staleness crawl → pack feedback)
worktree: /Users/sac/frozen-duckdb-wt/g7
branch: feat/155-g7
---

# G7 — workflows + projection docs crawl

## Mission

Falsify every claim in your slice against current reality. Outdated → fix at the
lawful source (pack facts → re-render for consequences; doc text in place for
prose). Missing → add or ledger. Feed pack-relevant findings into
docs/sjira/v26.9.21/PACK_FEEDBACK.md (append your section; create with header if
absent). 偽: a claim you did not test is UNKNOWN, not true.

## Scope

```
.github/workflows/**; docs/sjira/v26.9.21/{CI_PROJECTION,ADMISSION}.md
```

## Contract (all tickets)

- Base f3120fd: all gates green (ggen sync run, make verify, make genesis-check, 301 tests). Keep them green.
- Work ONLY in `/Users/sac/frozen-duckdb-wt/g7` on `feat/155-g7` (G9: marketplace worktree per ticket). Never push. Atomic conventional commits.
- Rendered consequences are edited at their source + re-rendered via `ggen sync run` — never hand-edited (exceptions must be ledgered UNSUPPORTED with the reason).
- Preserve command+exit evidence. Append transitions to the History table below (sanctioned main-checkout write), then STOP — the coordinator merges.

## History

| ts | standing | branch+SHA | gates+exits | remaining |
|----|----------|------------|-------------|-----------|
| 2026-09-21 | OPEN | feat/155-g7@f3120fd | not started | all of DoD |
| 2026-09-21 | ALIVE | feat/155-g7@4cebccb | WORKFLOW VERDICTS — TR1 claims TRUE: ci.yml fail-fast:false + timeouts 45/30/60 + rustfmt,clippy components + pre-seed (upstream v1.5.5 zips libduckdb-linux-{amd64,arm64}.zip / libduckdb-osx-universal.zip all HTTP 200, DUCKDB_VERSION=1.5.5 == builder const VERSION); ci-simple/test-minimal same pre-seed + components + timeouts 45/20; prebuilt/+setup_env.sh exist. TR2 claims TRUE: build-binaries CMAKE_OSX_ARCHITECTURES per matrix arch, action-gh-release@v2, mkdir -p fixes (src+stage), asset names libduckdb_{arch}.dylib end-to-end (find/upload/release paths == builder release_asset_name). 2 STALE claims fixed (comments only): post-TR7 fallback text ("ignores cmake/make failures" — now checked; "builder only symlinks libduckdb.dylib" — platform-correct post-1865954) and "TR7 being taught/until that lands" (landed; uname arch detection/placement still fails x86_64 leg closed at find — builder-owned residual). MECHANICAL FIXES: actions/cache v3→v4 (actionlint "runner too old"; live-runner proof = next PR run, agent never pushes); build-binaries timeout-minutes 150/15 (ci.yml bounded-failure law); workflow_dispatch version-input guard (input was silently ignored vs builder const VERSION pin). GATES: yaml-parse 4/4 OK; actionlint exit 0 CLEAN (found cache@v3 ×2 pre-fix); bash -n every run block ALL OK; gh api live CI master a3a69e4: CI 35674110999 + CI Simple 35674111087 + Test Minimal 35674111126 ALL conclusion=success (8/8 jobs incl. both beta legs, ~2.5min each); no releases/tags exist (404-comment claims TRUE); ggen sync run exit 0 ×2; cargo build --workspace exit 0 (builder consumed cached libduckdb_arm64.dylib live) | coordinator merge |
| 2026-09-21 | ALIVE | feat/155-g7@4cebccb | DOC VERDICTS — CI_PROJECTION FE-1..FE-5 ALL STILL TRUE vs ggen 26.8.18 (ggen --version unchanged; pack v0.3.2 unchanged since 97e630cb9 2026-09-10): FE-1 re-proven LIVE (transient [[ontology.pack]] → ggen sync run exit 1 [FM-CONFIG-003] unknown field `pack` expected source/imports/base_iri/prefixes/standard_only; config restored clean); FE-2..5 source-confirmed (gate 020 40-hex branch; no gha:withBlock grep=0; unconditional permissions:; GROUP_CONCAT mechanism); dated re-verification section appended. ADMISSION updated to pack-exists reality: frozen-duckdb-pack v0.1.0 EXISTS on pack/frozen-duckdb-capabilities @301ebc42f (pack.toml+ontology+2 templates+3 rq gates+1 py structural gate+qualification/consumer.ttl; = §2 shape minus optional targets.toml); absent from main (git show → path does not exist); court never ran, standing UNKNOWN; ledger-row rule sharpened (existence ≠ row retirement). PACK_FEEDBACK.md created + G7 section (4 evidenced findings: paydown path still owed; pack pre-admission state; action-version rot gap cache@v3; builder arch-detection vs CMAKE_OSX_ARCHITECTURES residual). 帳: all lines in declared scope; build-binaries remains TR2's UNSUPPORTED row (mine +~33 lines there, same missing capability/owner, paydown unchanged); no new UNSUPPORTED rows. Operator did NOT write: all of the above | coordinator merge |
