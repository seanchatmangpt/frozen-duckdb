---
id: T3
dcterms:title: "Examples compile + run"
dcterms:created: "2026-09-21"
dcterms:isPartOf: "v26.9.21"
rdf:type: oslc_cm:ChangeRequest, earl:TestRequirement
aps:standing: https://w3id.org/chatman/aps#PARTIAL_ALIVE
milestone: frozen-duckdb v1.5.5 (DuckDB 1.5.5 support, closes seanchatmangpt/frozen-duckdb#1)
worktree: /Users/sac/frozen-duckdb-wt/examples
branch: feat/155-examples
---

# T3 — Examples compile + run

## Scope (files you may change)

```
crates/frozen-duckdb/examples/{basic_usage,performance_comparison}.rs
```

Anything outside scope: do not edit — record BLOCKED with the reason instead.

## Work order


Context: 'use duckdb::' rot. Fix to frozen_duckdb::, build all examples, then EXECUTE basic_usage against the frozen 1.5.5 dylib (execution evidence, not inspection). performance_comparison may be slow — cap with timeout 300.
DoD: [ ] cargo build --workspace --examples → exit 0  [ ] cargo run --example basic_usage → exit 0, output in History  [ ] cargo run --example performance_comparison → exit 0 or BLOCKED with preserved output

## Contract (all tickets)

- Work ONLY in `/Users/sac/frozen-duckdb-wt/examples` on `feat/155-examples`. NEVER touch the main checkout /Users/sac/frozen-duckdb or sibling worktrees. Never push, never touch remotes.
- Atomic commits, conventional messages (`test:`, `fix:`, `docs:`, `ci:`, `build:`), on your branch only.
- 流: read this ticket → 階 (reuse what exists) → edit → verify (run the DoD gates) → 証 (append History) → stop.
- Standing vocabulary: ALIVE | BLOCKED | BUILD_BROKEN | PARTIAL_ALIVE | REFUSED_* | UNKNOWN | UNSUPPORTED. Preserve every command + exit code. inspection ≠ execution: a gate you did not run is not green.
- Shared resources: ~/.frozen-duckdb/cache (read-mostly; T7 owns its v1.5.5-arm64 mutation), crates.io registry, network. Coordinate through the ticket, never by touching sibling trees.
- Update the History table on every transition, then STOP — the coordinator merges.

## History

| ts | standing | branch+SHA | gates+exits | remaining |
|----|----------|------------|-------------|-----------|
| 2026-09-21 | OPEN | feat/155-examples@c8e5788 | not started | all of DoD |
| 2026-09-21T21:05Z | PARTIAL_ALIVE | feat/155-examples@958e676 | fixed `use duckdb::` → `use frozen_duckdb::` in basic_usage.rs + performance_comparison.rs; commit 958e676; gate1 `cargo build --workspace --examples` → exit 0 (all 4 examples compile incl. dropin_replacement + flock_ollama_integration); gate2 `cargo run --example basic_usage` → exit 134: `dyld[80643]: Library not loaded: @rpath/libduckdb.dylib` — example binaries have 0 LC_RPATH | gate2/gate3 run evidence blocked on rpath defect |
| 2026-09-21T21:15Z | PARTIAL_ALIVE | feat/155-examples@958e676 | ROOT CAUSE (out of scope, not edited): crates/frozen-duckdb/build.rs:15 (base ac9c5e3) emits `cargo:rustc-link-arg-example` — instruction does not exist; documented form is plural `rustc-link-arg-examples`; legacy `cargo:` maps unknown keys to DEP_ metadata silently → rpath never reaches example links. Receipts: emitted line present in target/debug/build/frozen-duckdb-2b946b0f2b41cae7/output; verbose rustc invocation for example has `-L native=<cache>` but no rpath arg; `frozen-duckdb-cli` bin HAS `LC_RPATH=/Users/sac/.frozen-duckdb/cache/v1.5.5-arm64` via `rustc-link-arg-bins`. DIAGNOSTICS (labeled, NOT DoD evidence): `DYLD_LIBRARY_PATH=<cache>` → basic_usage exit 0 (full run: connect, create users, insert 2 rows, query Alice/Bob, 1000 queries in 225ms/4443 qps, completed successfully); performance_comparison exit 0 under `timeout 300` (10000 rows in 219ms/45580 rows/s, all 5 queries, batch 100 rows, completed) → example code + 1.5.5 engine sound; sole defect is example-link rpath. Honest gates preserved: `./target/debug/examples/performance_comparison` → exit 134, same dyld error as gate2 | BLOCKED on coordinator: fix = one word, build.rs line 15 `-example=` → `-examples=` (outside T3 scope per ticket contract — recorded, not edited); after fix lands, rerun `cargo run --example basic_usage` + `performance_comparison` (no env) for true DoD evidence |
