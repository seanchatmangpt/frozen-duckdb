---
id: G4
dcterms:title: "GENESIS + HANDWRITTEN accuracy crawl"
dcterms:created: "2026-09-21"
dcterms:isPartOf: "v26.9.21"
rdf:type: oslc_cm:ChangeRequest, earl:TestRequirement
aps:standing: https://w3id.org/chatman/aps#OPEN
milestone: frozen-duckdb v1.5.5 wave 5 (gap/staleness crawl → pack feedback)
worktree: /Users/sac/frozen-duckdb-wt/g4
branch: feat/155-g4
---

# G4 — GENESIS + HANDWRITTEN accuracy crawl

## Mission

Falsify every claim in your slice against current reality. Outdated → fix at the
lawful source (pack facts → re-render for consequences; doc text in place for
prose). Missing → add or ledger. Feed pack-relevant findings into
docs/sjira/v26.9.21/PACK_FEEDBACK.md (append your section; create with header if
absent). 偽: a claim you did not test is UNKNOWN, not true.

## Scope

```
docs/GENESIS.md; HANDWRITTEN.md; .gitignore
```

## Contract (all tickets)

- Base f3120fd: all gates green (ggen sync run, make verify, make genesis-check, 301 tests). Keep them green.
- Work ONLY in `/Users/sac/frozen-duckdb-wt/g4` on `feat/155-g4` (G9: marketplace worktree per ticket). Never push. Atomic conventional commits.
- Rendered consequences are edited at their source + re-rendered via `ggen sync run` — never hand-edited (exceptions must be ledgered UNSUPPORTED with the reason).
- Preserve command+exit evidence. Append transitions to the History table below (sanctioned main-checkout write), then STOP — the coordinator merges.

## History

| ts | standing | branch+SHA | gates+exits | remaining |
|----|----------|------------|-------------|-----------|
| 2026-09-21 | OPEN | feat/155-g4@f3120fd | not started | all of DoD |
| 2026-09-21 | ALIVE | feat/155-g4@c2f4432 (5 commits: 24a20bb secrets-fix, 42c85c0 PACK_FEEDBACK, 6ececc1 GENESIS reconcile, f9491bb HANDWRITTEN, c2f4432 evidence-ttl) | `make genesis-check` exit 0 (checked=58 deferred=0 gaps=23 failed=0; was 37/0/8); `make verify` exit 0 (build/clippy/fmt/test-validation/test-workspace all exitCode 0, sync exit 0 with 11 outputs, receipt verify valid=true signed=true signature_valid=true); `cargo build --workspace` exit 0. Falsifiers: (1) fresh-clone key removal — rm -rf .ggen .ggen-v2 → `make gates` exit 0 + `ggen receipt verify` exit 0 (sync regenerates keypair; untracking is safe); (2) lineage-row bite — moved docs/explanation/related.md aside → genesis-check exit 2 "FAIL [gap] … (gap file missing)", restored → exit 0. Verification: 45/45 pre-existing row paths exist; 41-file vendored family count + gitlink f4fe688a8718 confirmed; commit-hash claims 304a958/400fba1/7a271c7/4c41d83/7f604e5 confirmed. Findings: C-tripwire flip + Makefile rule-ownership confirmed as dry-run agent left them; HANDWRITTEN Makefile row confirmed paid down; C2 renders PRESENT (falsified GENESIS "absent from this branch" — merged via cae1a4d) → 15 lineage:7f604e5 gap rows; CHANGELOG row re-pointed to ggen rule `changelog` (a09ce8a); SECRETS DEFECT: .ggen/keys/{signing,verifying}.key tracked since 30b14fc → untracked + gitignored; dual-owner hazard: release_checklist.sh still invokes superseded generate-changelog.sh (PACK_FEEDBACK G4 #1). 比: hand-written bytes this change = ledgers/docs/gitignore prose only (manifest rows document generator ownership; zero 産面 code touched) | none — STOP, coordinator merges |
