<!-- GENERATED by ggen from readme-diataxis-pack. -->
<!-- Edit admitted RDF/template source, not this projection. -->

# How to deploy

To tag the release, run:

```bash
git tag v1.5.5
```

To push the tag to trigger the release build, run:

```bash
git push origin v1.5.5
```

